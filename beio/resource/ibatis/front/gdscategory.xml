<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="gdscategory">
	
	<select id="queryCategory" parameterType="GdsCategory" resultType="GdsCategory">
		SELECT * FROM gds_category WHERE enable = '1' AND exist = '1' 
		<if test="id != null and id != ''">
			AND id = #{id}
		</if>
		ORDER BY level + '' ASC;
	</select>
	
	<select id="queryIndexHotCategory" parameterType="GdsCategory" resultType="IndexHotGoodsVO">
		SELECT * FROM gds_category WHERE enable = '1' AND exist = '1' AND isShow = '1' AND level = '1';
	</select>
	
</mapper>